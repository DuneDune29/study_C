#include <stdio.h> 

int bigNum(int num01, int num02) {
  if(num01 >= num02)
    return num01;
  else
    return num02;
}

int main(void) {
  int result;

  result = bigNum(7, 5); // 함수 호출
  printf("큰 수는 %d \n", result);
  return 0;
}
