#include <stdio.h>

int main(void) {
  int len = 0;
  char str[] = "string";

  while (str[len] != '\0') {
    len++;
  }

  printf("문자열 %s의 길이는 %d \n", str, len);
  return 0;
}
------------------------------------------
문자열 string의 길이는6
