#include <stdio.h>

int main(void) {
  int i, sum = 0;
  int grade[3] = { 85, 65, 90 };
  // 길이가 3인 int 배열의 선언과 동시에 초기화

for(i = 0; i <3; i++){
  sum += grade[i];
}

printf("합계는 %d이고, 평균은 %f \n", sum, (double) (sum/3));
  return 0;
}
